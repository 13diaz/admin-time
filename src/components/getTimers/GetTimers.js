/* import { useLocalStorage } from "../../utils/hooks/useLocalStorage";

const GetTimers = (itemName, initialValue) => {
  const [timers, setTimers] = useLocalStorage(itemName, initialValue);

  // return [timers, setTimers];
};

export default GetTimers; */
